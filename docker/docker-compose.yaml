services:
  backend:
    build:
      context: ..
      dockerfile: docker/Dockerfile
      target: runtime
    container_name: lingua_backend
    restart: unless-stopped
    networks: [web]
    env_file:
      - ../.env
    ports:
      - "3010:3000"

networks:
  web:
    external: true